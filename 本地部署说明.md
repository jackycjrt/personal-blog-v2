# Hugo 博客本地部署说明（Windows）

本说明仅用于在本机部署与运行当前博客项目，不设置任何快捷方式。

## 1. 环境准备

1. 安装 Hugo Extended（建议 0.120 及以上版本）。
2. 打开 PowerShell，执行：

   hugo version

3. 能看到版本号即表示 Hugo 可正常使用。
4. 当前本机已验证版本：0.159.1 Extended。

## 2. 进入项目目录

在 PowerShell 中执行：

Set-Location "e:\自己项目\个人博客搭建\版本二"

## 3. 本地启动预览

执行：

hugo server --disableFastRender

默认访问地址：

http://localhost:1313

说明：

1. 这是本机即时预览模式，修改内容后会自动刷新。
2. 关闭终端后服务停止，不会残留后台快捷启动项。

## 4. 亮暗模式说明

1. 顶部导航右侧提供亮暗切换按钮。
2. 默认模式由 config.toml 中 params.defaultTheme 控制（当前为 auto）。
3. 用户手动切换后会写入浏览器本地存储，下次访问保持上次模式。

## 5. 内容目录规范

1. 博客文章：content/posts
2. 项目页面：content/projects
3. 杂记页面：content/notes
4. 关于页面：content/about/index.md
5. 文章模板：archetypes/default.md

## 6. 主题与配置位置

1. 站点配置：config.toml
2. 主题样式：themes/anthropic-minimal/assets/css/main.css
3. 主题脚本：themes/anthropic-minimal/assets/js/main.js
4. 模板文件：themes/anthropic-minimal/layouts

## 7. 生产构建

执行：

hugo --minify

构建产物目录：

public

## 8. 常见问题

1. 页面无样式：确认 themes/anthropic-minimal/assets 下存在 css 与 js 文件。
2. 菜单不显示：确认 config.toml 中 menu.main 配置完整且 pageRef 正确。
3. 标签页空白：确认文章 front matter 中 tags 字段非空。
4. 主题未生效：确认 config.toml 中 theme = "anthropic-minimal"。
5. 命令找不到：若 winget 已安装但 `hugo` 无法识别，检查 `C:\Users\Ycjrt\AppData\Local\Microsoft\WinGet\Links\hugo.exe` 是否存在，并将该目录加入用户 PATH。

## 9. 核心日常工作流（从 Obsidian 到线上）

为了实现“在 Obsidian 写作 -> 本地预览 -> 发布到 GitHub Pages”，推荐走以下标准流程：

### 第一步：在 Obsidian 写作
将文章写在以下配置的发布源目录中：
`C:\Users\Ycjrt\OneDrive\应用\remotely-save\yu\40_知识库\Blog`

建议把文章分门别类放在以下三个一级文件夹里（中文优先）：
- `文章` 或 `posts` -> 将会发布到博客的“博客”菜单
- `项目` 或 `projects` -> 将会发布到博客的“项目”菜单
- `杂记` 或 `notes` -> 将会发布到博客的“杂记”菜单

*(注意：图片可以直接放在文章旁边，同步时图片会自动带走；文章只要被同步，会自动标记为可发布且取消草稿状态。)*

### 第二步：将文章同步到本地并预览
写完后，在当前项目根目录打开 PowerShell终端（如果你在 CMD，必须在命令前加上 `powershell `）：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\sync-obsidian-to-hugo.ps1 -RunBuild
```

该命令会自动把文章复制过来，并更新项目下的 `content/posts/obsidian` 等目录。
同步完成后，如果要看本地效果，执行：

```powershell
hugo server --disableFastRender
```

在浏览器打开 `http://localhost:1313` ，确认新文章是否已经出现在博客页面上。

### 第三步：一键发布到 GitHub Pages
确认本地效果没问题后，在 PowerShell 中一键发布：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\publish.ps1
```

该脚本会自动执行：**再次同步 Obsidian -> Hugo 构建 -> Git 提交 -> 推送 main 分支**。
推送成功后，GitHub Actions 会在云端自动重新构建和发布，大约 1-3 分钟后，公开站点就会更新。

---

## 10. 详细同步参数与工作流机制说明

已按当前需求配置以下参数：

1. Obsidian Vault 路径：C:\Users\Ycjrt\OneDrive\应用\remotely-save\yu
2. 发布源目录：C:\Users\Ycjrt\OneDrive\应用\remotely-save\yu\40_知识库\Blog
3. 图片策略：跟文章同目录（同步时保留目录结构）
4. 元信息规则：固定字段，默认不再强制草稿（`draft = false`）
5. 安全策略：先导出到临时目录，且不会清空全站历史内容

推荐在 Obsidian 发布源目录下使用以下一级文件夹（中文优先）：

1. 文章 -> 同步到 content/posts/obsidian
2. 杂记 -> 同步到 content/notes/obsidian
3. 项目 -> 同步到 content/projects/obsidian

兼容英文目录：

1. posts/post/blog/tech/study -> posts
2. notes/note/life/daily/journal -> notes
3. projects/project -> projects

执行同步（并自动构建）命令：

PowerShell -ExecutionPolicy Bypass -File .\scripts\sync-obsidian-to-hugo.ps1 -RunBuild

同步产物目录（由脚本托管）：

1. content/posts/obsidian
2. content/notes/obsidian
3. content/projects/obsidian

## 11. GitHub Pages 自动发布

已创建工作流文件：

1. .github/workflows/deploy-pages.yml

触发方式（按需求 C）：

1. 推送到 main 自动发布
2. GitHub Actions 页面手动触发 workflow_dispatch 发布

## 12. 初始化云端仓库

仓库将创建在账号 jackycjrt 下，默认仓库名：personal-blog-v2。

先登录 GitHub CLI：

gh auth login

然后执行初始化脚本：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\init-github-pages.ps1
```

脚本会自动完成：

1. 初始化本地 git 仓库（若尚未初始化）
2. 建立首次提交
3. 创建远程仓库并绑定 origin
4. 推送 main 分支

---
*注：日常发布只需执行 第 9 节 的第三步 `publish.ps1` 即可，不用关心中间环节。*
